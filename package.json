{
  "name": "spring-bean-navigation",
  "displayName": "Spring Bean Navigation",
  "description": "Navigate Spring Bean dependencies with CodeLens hints",
  "version": "0.0.1",
  "publisher": "xtdw12345",
  "repository": {
    "type": "git",
    "url": "https://github.com/xtdw12345/spring-bean-navigation"
  },
  "engines": {
    "vscode": "^1.107.0"
  },
  "categories": [
    "Programming Languages"
  ],
  "activationEvents": [
    "onLanguage:java",
    "workspaceContains:**/pom.xml",
    "workspaceContains:**/build.gradle"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "spring-bean-navigation.navigateToBean",
        "title": "Spring Bean Navigation: Navigate to Bean"
      },
      {
        "command": "spring-bean-navigation.rebuildIndex",
        "title": "Spring Bean Navigation: Rebuild Bean Index"
      }
    ],
    "configuration": {
      "title": "Spring Bean Navigation",
      "properties": {
        "spring-bean-navigation.indexing.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable/disable Spring Bean indexing"
        },
        "spring-bean-navigation.indexing.paths": {
          "type": "array",
          "default": [
            "src/main/java",
            "src/test/java"
          ],
          "description": "Paths to index for Spring Beans (relative to workspace root)",
          "items": {
            "type": "string"
          }
        },
        "spring-bean-navigation.indexing.excludePatterns": {
          "type": "array",
          "default": [
            "**/target/**",
            "**/build/**",
            "**/.git/**",
            "**/node_modules/**"
          ],
          "description": "Glob patterns to exclude from indexing",
          "items": {
            "type": "string"
          }
        },
        "spring-bean-navigation.indexing.maxCacheSize": {
          "type": "number",
          "default": 20,
          "description": "Maximum cache size in MB for Bean index"
        },
        "spring-bean-navigation.indexing.debounceDelay": {
          "type": "number",
          "default": 500,
          "description": "Debounce delay in milliseconds for file change events"
        },
        "spring-bean-navigation.indexing.showProgress": {
          "type": "boolean",
          "default": true,
          "description": "Show progress notification during indexing"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test",
    "test:coverage": "c8 --reporter=text --reporter=html --exclude='out/test/**' --exclude='**/*.test.js' npm test"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.107.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "c8": "^10.1.3",
    "eslint": "^9.39.1",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.48.1"
  },
  "dependencies": {
    "java-parser": "^3.0.1"
  }
}
